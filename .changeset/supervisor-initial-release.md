---
'@platformos/platformos-mcp-supervisor': patch
---

Initial npm release of the platformOS MCP supervisor — a stdio MCP server exposing `validate_code` so AI agents can validate platformOS Liquid/GraphQL/YAML files before writing them. Ships as a native ES module.
