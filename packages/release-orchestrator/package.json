{
  "name": "release-orchestrator",
  "version": "0.0.9",
  "description": "Internal platformos-tools package for handling the release process.",
  "main": "dist/index.js",
  "author": "platformOS",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "yarn build:ts",
    "build:ci": "yarn build",
    "build:ts": "tsc -b tsconfig.build.json",
    "start": "yarn build && node dist/index.js",
    "type-check": "tsc --noEmit"
  }
}
